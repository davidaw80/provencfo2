# ProvenCFO Marketing Site

## Usage

Once the repo has been cloned onto desktop simply navigate into the root directory and run:

jekyll serve

or to do real-time updates on the localhost run:

jekyll serve --incremental
